apiVersion: v1

kind: Service

metadata:
  name: healthcare-frontend-service

spec:
  type: NodePort

  selector:
    app: healthcare-frontend

  ports:
    - port: 5173
      targetPort: 5173
      nodePort: 30173